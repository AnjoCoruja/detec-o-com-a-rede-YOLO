🚀 Detecção de Objetos com YOLO (You Only Look Once)
Este repositório contém uma implementação prática para detecção de objetos em tempo real utilizando a arquitetura YOLO. O foco é demonstrar a capacidade da rede de identificar e localizar múltiplas classes em imagens e vídeos com alta performance.

📌 Sobre o YOLO
Diferente de algoritmos tradicionais (como R-CNN) que aplicam o modelo em várias regiões da imagem, o YOLO "vê" a imagem inteira uma única vez. Ele divide a imagem em uma grade e prevê as caixas delimitadoras (bounding boxes) e as probabilidades de classe simultaneamente.

Principais Vantagens:
Velocidade Extrema: Ideal para aplicações em tempo real (FPS alto).

Contexto Global: Menos erros de detecção em planos de fundo por processar a imagem completa.

Generalização: Aprende representações genéricas de objetos, facilitando a aplicação em novos domínios.
